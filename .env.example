# Environment Variables - NIEMALS committen!
# Kopiere diese Datei zu .env und fülle die Werte aus

# Flask Secret Key (generiere mit: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=generate-with-secrets-token-hex-32

# Email Configuration
EMAIL_PASSWORD=your-email-password-here

# Database Path (optional)
DATABASE_PATH=data/database.db

# Optional: Ollama URL
OLLAMA_URL=http://localhost:11434
